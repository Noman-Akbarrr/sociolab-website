[phases.setup]
nixPkgs = ["nodejs_22", "openssl"]

[phases.install]
cmds = ["npm ci --ignore-scripts"]

[phases.build]
cmds = ["npx prisma generate", "npm run build"]

[start]
run = "npx prisma db push --skip-generate && node ./node_modules/next/dist/bin/next start"
